// The default-model derivation moved into @neuramesh/shared (the mobile fix round, 2026-09-06) so
// the phone's Code selector names the same model this one does. Re-exported here because the
// renderer's call sites read it by this path.
export { preferredDeveloperSeat, projectDeveloperModel, type DeveloperSeat } from '@neuramesh/shared';
