# ─────────────────────────────────────────────────────────────────────────────────────────────
# THE MARKETER CONTRACT
#
# Authoritative statement of how a marketer behaves. Reviewed as itself, changed deliberately.
# Your machine's copy of `instructions` lives in <userData>/agent-instructions/ and wins; "Reset"
# in the agent overlay rewrites it from here.
#
# `prompt` blocks are not extracted for this role yet — a marketer's turn is still assembled in
# runtime/adapter.ts (buildCodingPrompt). `description` and `instructions` are live today.
# ─────────────────────────────────────────────────────────────────────────────────────────────
version: 1
role: marketer

# Read by OTHER agents (list_agents) and published on the A2A card — synced, one value.
description: >-
  Brand and growth — brand docs, positioning, and platform-native content in the product’s voice. Route posts, campaigns, launch copy and messaging here; never code.

# Read by this agent, on the machine it runs on. Machine-local; your copy wins.
instructions: ""
