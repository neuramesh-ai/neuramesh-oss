# Code end-to-end validation — 2026-08-31

This evidence was captured from the uncommitted `codex/engineering-os` worktree. Nothing was
committed, pushed, merged, deployed, or opened as a pull request.

## Real web workflow

- Current live URL: `http://127.0.0.1:5202/acme?db=engineering-1788219720`
- Stack: local web client + Clerk test token + control API + relay + machine edge + Docker
  Postgres/PowerSync
- Model route: OpenAI Core
- Effective developer model: `gpt-5.5`
- Project/repository: `default` / `e2e-local`
- Initial mode: Plan

The prompt asked Code to create a small testable `pomodoro_cycles` API, add focused tests, avoid
the existing greeting contract and every JavaScript file, inspect first, and remain read-only in
Plan. Plan inspected the repository and proposed only the Pomodoro implementation. The one-click
handoff continued the same task in Act, where Code proposed and applied:

- `src/pomodoro.py`
- `test/test_pomodoro.py`

The real machine worktree was independently checked after the turn. `git status
--short --untracked-files=all` contained only those two paths—no `src/greeting.mjs` change and no
generated bytecode. Verification passed:

```text
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s test -p 'test_*.py'
Ran 3 tests
OK
```

The web walkthrough also verified:

- ＋ creates and selects a new Code task without a stale rail selection reclaiming focus;
- project and Model pickers use the shared NeuraMesh popup/chip vocabulary;
- project selection determines the repository used by the new task;
- the attachment control and shared attachment tray are present in the real composer;
- provider reasoning streams in one disclosure and collapses when answer/tool output begins;
- the Plan handoff switches mode and resumes with one click;
- edits and commands remain separately approval-gated;
- the Changes pane is reconciled from the authoritative worktree, including command-created files;
- the primary approval remains oak before and during hover in Graphite.

The OS-native file chooser cannot be driven through the embedded-browser automation boundary.
Attachment transport was therefore validated below that chooser with focused integration tests:
ordered relay chunks are reconstructed outside the repository, routed to SDK `userFiles` or
`userImages`, observed by the provider turn, bounded by count/size/total limits, and cleaned up.

## Composer and model-picker follow-up

The live web harness was rechecked at the narrow 320px Code-pane width after the follow-up review:

- `web-code-model-selector.png` — Code now exposes the one execution model it actually uses,
  grouped by provider. The configured Claude line is available, the unconfigured ChatGPT/Gemini
  lines remain visible and disabled, and each unavailable provider has a Connect action;
- `web-code-model-connect-settings.png` — Connect opens Workspace settings directly on Providers
  and focuses the requested provider card;
- `web-code-home-flat-recents.png` — recents are flat notification-style rows rather than a stack
  of elevated cards;
- `web-code-tooltip-bounds.png` — the attachment tooltip opens inward. Its measured horizontal
  bounds were 314–449px inside the Code pane's 291–611px bounds.

The latest Code landing pass also verifies notification-weight thread typography, explicit project
labels, hollow Plan versus solid Act history markers, and one short center resize lever instead of
the previous pair of full-height divider rules. The Model picker names the resolved developer model
when it is known, marks it as the Project default, and presents the managed starter only as
`NM Cloud Starter v1` / `NeuraMesh managed`—never as its changeable backing model id.
The routine `Code connected` success row is now absent: ready is the default, while checking and
unavailable states retain the same surface because they explain a temporarily disabled composer.
`code-home-no-connected-web.jpg` records the live authenticated web result.

The rendered browser assertions confirmed 14 catalog rows, seven disabled unavailable models, two
provider Connect actions, and no document overflow at the narrow 320px Code-pane width. Selecting
Claude Sonnet 5 changed the per-thread control immediately; resetting to Project default restored
inheritance. After refreshing the local machine process to the current protocol, the real relay
resolved that reset to Claude Opus 4.8 and the picker rendered `Claude Opus 4.8` / `Project default
· developer model` / `Current`. The popup measured 296px inside the pane's 320px bounds.

## Desktop/theme workflow

The production preview renderer ran the deterministic complete workflow in Graphite and Cream Oak:

1. Code landing, recent tasks, and the direct Model catalog (`engineering-model-selector-desktop`
   in both themes);
2. Plan result and one-click Act handoff;
3. complete two-file diff before approval;
4. brand-colored Approve once action;
5. applied changes, verification, and restorable checkpoints.

Both runs passed the Electron capture assertions. The composer was held at its narrow default so
the Plan/Act, project, and Model control row also proves independent truncation without overlap.

## Automated verification

- focused Code protocol/host/workspace/relay/domain/navigation/UI suite: 78/78;
- full desktop suite: 1,127/1,127;
- TypeScript: pass;
- desktop production build: pass;
- web production build: pass;
- local PowerSync schema, connections, and sync rules: pass;
- desktop Graphite E2E capture: pass;
- desktop Cream Oak E2E capture: pass.

Vite still reports the repository's pre-existing malformed marketing scroll-point CSS block during
web/preview minification. It predates and is unrelated to Code; the build succeeds.
